# AGENTS.md — よだかの星 手打ちうどん(引き継ぎ書)

架空のうどん店「よだかの星」を題材にした店舗サイト制作例。GitHub Pagesで公開中だが、実店舗の営業・予約窓口ではない。
このファイルは作業を引き継ぐエージェント向けの起点。**まず本ファイル → 次に `README.md` を読むこと。**

## プロジェクト概要
- 静的サイト(HTML/CSS/JS のみ・ビルド不要・依存ライブラリなし)
- 本体は `site/` 配下。トップ + 下層4ページ(メニュー/こだわり/店舗案内/お問い合わせ受付案内)+ 非導線のサンクスページ
- 公開URL: `https://techuueda-bot.github.io/yodaka-no-hoshi-udon/`
- 電話予約・フォーム送信・実在地点の地図は停止中。架空の番号に `tel:` を付けない
- デザイン方向: 「活字と丼 — 老舗×文学」。宮沢賢治「よだかの星」の世界観を、京都の仕出し店サイト「菱岩」(https://hishiiwa.com)の思想で表現

## 動かし方
```bash
cd site
python3 -m http.server 8420
# http://localhost:8420/ を開く
```

## QA(変更したら必ず通す)
```bash
# スキルのスクリプト。パスは環境に合わせて調整
python3 <web-productionスキル>/scripts/qa_check.py  site/
python3 <web-productionスキル>/scripts/visual_qa.py site/   # Playwright。3幅スクショ→ visual-qa/
```
現状は両方 FAIL 0 / WARN 0。

## ★ 触ってはいけない設計上の制約(過去の議論・施主フィードバックで確定)
1. **具象イラスト禁止**。料理・人・建物・地図を「絵」で描かない。実物の代理をベクターで描くとクリップアート化し、施主に却下された。許されるSVGは意匠のみ(星図・罫線・印章・一筆書きの線画・家紋・紙グレイン)。写真素材は無い前提で、タイポグラフィと余白で語る。
2. **実写真は未使用**。`/menu/`の全12品には、料理行を選ぶと開く「イメージ写真（生成）」を用意している。実店舗の料理写真として扱わず、一覧と写真ビューアの双方で生成表記をする。将来、権利確認済みの実写真が用意できたら、料理ごとに利用許諾とメニュー内容を確認して差し替える。想定位置と品質条件はREADME参照。
3. **ファーストビューを遅らせる演出は禁止(0秒)**。ローディング/スプラッシュの類は入れない。→ 施主も一度検討し「入れない」と決定済み。
4. **常時動作するアニメは「ヒーローの星の明滅」1つだけ**。他の動きはすべて「視界に入った時に一度きり・1.2s以内」。マーキー・自動スライダー・カスタムカーソル・スクロール視差は入れない。
5. **色はトークンの範囲内**(生成り #f7f2e6 / 墨 #262622 / 濃紺 #1f2d3d / 金茶 #b8863f / 薄罫 #ddd0af / muted #766f5f)。塗り面・グラデ・ピル形状・ベタ塗りボタン禁止。金茶は小さな記号・ラベルのみ。
6. **時間で自動的に変わる事実の表示禁止**(JSの曜日判定で「営業中」等を出さない。祝日・臨時休業で嘘になる)。曜日帯は静的表示+「火曜定休」テキスト併記。
7. **すべての動きは `motion-stop` ボタン(と reduced-motion)で停止できること**。新しい動きを足したら停止ポリシーに接続する。
8. デザイン基盤は `assets/css/style.css`(トークン→レイアウト→コンポーネント→モーション→停止の順)と `assets/js/main.js`(IntersectionObserver + CSS transition のみ。ライブラリ追加しない)。

## 実装済み(要約。詳細は README.md)
- タイポgrafィ主導のエディトリアル。縦書きヒーロー、縦書き×円環の見出し、巨大一文字の背景、手彫り印章ロゴ、紙グレイン
- 季節限定は星図SVG(点と細線)、こだわりは巨大一文字、店主はテキスト引用
- `/menu/`の全12品に生成イメージ写真を追加。料理名・価格の行を選ぶと共有ビューアで1枚を開き、前後の料理へ移動できる。写真上に文字は重ねず、一覧とビューアで「イメージ写真（生成）」を明記
- 「賑わい」強化9項目(3体エージェント討論の裁定): お知らせ欄・地図掲載待ち表示・フッター再設計(曜日帯含む)・下層の巨大一文字・漢数字の献番・罫線描画アニメ・巨大一文字リビール・morelinkホバー・星の明滅
- 全6HTMLで「架空店舗サイト／制作例」を明示。OGP・canonical・構造化データは公開URL基準

## 決定済み(蒸し返さない)
- **文字サイズは調整済み(2026-07-13)**: 施主の「文字が小さい」指摘を受け、可読性を底上げ。本文16px(旧15)/行間2.0/字間0.04em、補助14px、最小ラベル11px、muted色を #766f5f→#635c4c に濃く、小サイズの字間を全体に詰めた。**ジャンプ率(見出しの大きさ)は維持**。これ以上小さくしないこと。
- **オープニング演出 = 入れない**で確定。

## 次にやる候補
- 実店舗化する場合のみ、正式な店舗情報と受付手段を確認して差し替える
- 権利確認済みの実写真3点が届いたら、READMEの写真差し替え仕様に従って追加する

## 実店舗サイトへ転用する前に必ず差し替え
`README.md`「実店舗サイトへ転用する場合」を参照。要点:
- 店舗情報(住所/電話/営業時間)・メニュー内容と価格・店主名・お知らせ
- `access/index.html` の掲載待ち表示を、確認済みの実住所Googleマップへ変更
- 正式な送信先とプライバシー文言を確定してからフォームとサンクスページを有効化
- お知らせ欄は「月1回更新する運用」とセットで公開(更新が止まるなら欄ごと外す)

## 公開手順
`main` へのpushで `.github/workflows/deploy-pages.yml` が `site/` をGitHub Pagesへ配信する。リポジトリ名のサブパスはワークフローでHTMLに付与し、CSS内画像は相対URLで参照する。
